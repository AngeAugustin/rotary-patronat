/** Conteneur horizontal partagé de l'espace public (header, pages, footer). */
export const publicContainerClass =
  'mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8';

/** Conteneur horizontal de l'espace connecté (header, barre latérale). */
export const dashboardContainerClass = 'px-4 sm:px-6 lg:px-8';

/** Hauteur commune de l'en-tête (barre principale + logo sidebar). */
export const dashboardHeaderHeightClass = 'h-[4.5rem] shrink-0';

/** Largeurs de la barre latérale (dépliée / repliée). */
export const dashboardSidebarExpandedWidthClass = 'w-64';
export const dashboardSidebarCollapsedWidthClass = 'w-[4.5rem]';

/** Contenu principal de l'espace connecté — largeur maîtrisée, aligné sur la vitrine. */
export const dashboardContentClass =
  'mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8';
